# A = ["abab", "ab", "abcd"]
# def calculate_common_prefix(A,B):
# 	common_str = ""
# 	n = min(len(A),len(B))
# 	for i in range(0,n):
# 		if A[i] != B[i]:
# 			break
# 		common_str += A[i]
# 	return common_str

# arr_len = len(A)
# common_prefix = calculate_common_prefix(A[0],A[1])
# for i in range(2, arr_len):
# 	common_prefix = calculate_common_prefix(common_prefix,A[i])
# print common_prefix


